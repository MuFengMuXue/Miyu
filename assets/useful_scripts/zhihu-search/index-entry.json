{
  "id": "zhihu_search",
  "display_name": "知乎搜索",
  "description": "检索中文问答平台——知乎。",
  "path": "zhihu-search",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "关键词，或知乎链接，或纯数字问题 id"
      },
      "limit": {
        "type": "integer",
        "description": "搜索结果条数(1-50，默认10)；query 是问题时表示取几条回答(1-20，默认5)"
      },
      "full": {
        "type": "boolean",
        "description": "搜索时是否连正文一起返回。默认在 limit<=5 时自动为 true。设 false 可强制只要摘要"
      },
      "format": {
        "type": "string",
        "enum": ["md", "json"],
        "description": "输出格式，默认 md(省 token 且易读)；需要逐字段处理时用 json"
      },
      "type": {
        "type": "string",
        "enum": ["general", "answer", "article", "people", "video"],
        "description": "搜索范围，默认 general(综合)。找人用 people"
      },
      "max_chars": {
        "type": "integer",
        "description": "每条正文最大字数。默认单条取全文 8000、多条 3000"
      },
      "action": {
        "type": "string",
        "enum": ["search", "question", "answer", "article", "doctor"],
        "description": "通常不用给——脚本按 query 自动判断。仅在需要强制(如把链接当关键词搜)或自检(doctor)时使用"
      }
    },
    "required": ["query"],
    "additionalProperties": false
  },
  "timeout_seconds": 300,
  "always_loaded": false,
  "load_policy": "group",
  "groups": ["research"]
}
