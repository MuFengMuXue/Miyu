=== load_tools [inline] 287B ===
  *names: array<string> -- Names of the tools, scripts, or tool groups to load. Only names from available_load_targets are allowed, e.g. web_search or group:gaming.

=== job [inline] 657B ===
   action: string enum[status,stop] -- status inspects, stop terminates. Defaults to status.
   all: boolean -- true reaches other sessions' jobs.
   job_id: string -- A single job id.
   job_ids: array<string> -- Several job ids at once; for status the log budget is split between them.
   offset: integer (minimum=0) -- status only: byte offset into the log, from the previous next_offset. The list mode's recent_output is a tail snippet, not a resume point.

=== query_token_usage [inline] 176B ===
   range: string enum[1d,7d,30d,all] -- Time range, defaults to 1d (rolling 24h).

=== share_file [inline] 755B ===
   path: string -- Absolute path (or ~/ relative to home) of the local file to share. Required for the share action.
   title: string -- Optional display title shown in the WebUI shared-files list.
   snapshot: boolean -- Copy the file into managed storage so the link keeps working even if the original file changes. Default false: serve the original file directly.
   action: string enum[share,list,unshare] -- Defaults to share. list returns all current shares; unshare removes one by share_id.
   share_id: string -- The share to remove. Required for the unshare action.

=== read_platform_file [inline] 195B ===
  *file: string -- File id from chat history, or a local path already under platform_files.

=== send_message_to_user [inline] 485B ===
   text: string -- Optional text or Markdown.
   images: array<object{path,alt}> (maxItems=4)
   files: array<object{path,name}> (maxItems=4)
   [分叉] 非管理员版: 无 files; images.path 描述="Path of an image generated by a tool this turn (e.g. generate_image). Other local paths are rejected."

=== qq_mention_users [inline] 271B ===
  *user_ids: array<string> (minItems=1, maxItems=32) -- Exact QQ IDs of the members to mention, in display order.

=== qq_withdraw_message [inline] 396B ===
   message_id: string|integer -- Required only when the current QQ message does not reply to a target. A trusted reply always overrides this argument. Never pass the current request message ID and never guess a recent message.
   reason: string (maxLength=500)
   confirmation_token: string

=== manage_platform_access [inline] 501B ===
  *operation: string enum[grant,revoke,list]
   permission: string enum[administrator,private_whitelist,group_whitelist]
   target_id: string -- QQ user id for administrator/private_whitelist, or QQ group id for group_whitelist. group_whitelist defaults to the current group when omitted. A single trusted mention or reply may identify a user when omitted.

=== save_current_message_meme [inline] 195B ===
   image_index: integer (minimum=1) -- 当前消息中的图片序号，从 1 开始；默认保存第一张。

=== delete_referenced_meme [inline] 62B ===
  (无参数)

=== get_real_chat_activity_ranking [inline] 614B ===
   days: integer (default=30) -- 最近天数；<=0 表示全部历史。指定 start_time 或 end_time 时忽略。
   limit: integer (default=20) -- 返回前几名；<=0 使用默认值 20，最大 200。
   start_time: string -- 可选开始时间：Unix 时间戳、RFC 3339、YYYY-MM-DD 或 YYYY-MM-DD HH:MM[:SS]。
   end_time: string -- 可选结束时间，格式同 start_time；仅日期时包含当天。
   include_bot: boolean (default=true)

=== search_real_chat_history [inline] 885B ===
   query: string (minLength=1) -- 关键词；省略则回放近期消息。
   sender_id: string -- 只看这个 QQ 号发的消息。
   user_id: string -- sender_id 的旧别名。
   conversation_kind: string enum[group,private]
   conversation_id: string -- 群号或私聊对方 QQ 号
   group_id: string
   all_conversations: boolean (default=false)
   all_groups: boolean (default=false)
   days: integer (minimum=1)
   start_time: string -- Unix 时间戳、RFC 3339、YYYY-MM-DD 或 YYYY-MM-DD HH:MM[:SS]
   end_time: string -- 格式同 start_time；仅日期时包含当天
   limit: integer (minimum=1, maximum=500)
   [注] limit.maximum 为运行时 history_limit_ceiling(settings)，默认配置下为 500

=== get_group_members_info [inline] 334B ===
  *query: string -- 完整或部分 QQ 号、群名片或昵称。
  *limit: integer (minimum=1, maximum=200) -- 本次最多返回多少条匹配结果，必须明确填写，当前上限为 200。
   [注] limit.maximum 与描述中上限值为运行时 group_member_search_max_results，默认 200

=== get_avatar [inline] 369B ===
   user_id: string (pattern=^[0-9]{5,20}$) -- 群成员的 QQ 号；省略时取当前群的群头像。只知道名字时先调用 get_group_members_info。
   download: boolean (default=false) -- true 时下载头像并发布为图片；默认只返回 URL。

=== delete_real_chat_history [inline] 869B ===
  *action: string enum[request,confirm]
   mode: string enum[all,keep_days]
   keep_days: integer (minimum=1)
   sender_id: string -- 仅删除此发送者 QQ 的消息
   conversation_kind: string enum[group,private]
   conversation_id: string -- 群号或私聊对方 QQ 号
   group_id: string
   all_conversations: boolean (default=false)
   all_groups: boolean (default=false)
   start_time: string
   end_time: string
   confirmation_token: string -- For action=confirm, use the opaque token returned by action=request. The current administrator message must also exactly equal the returned confirmation phrase.

=== qq_group_manage [inline] 1216B ===
  *action: string enum[mute,kick,title] -- mute 禁言/解禁，kick 踢人，title 设置群头衔。
   user_id: string -- QQ 号；可用空格或逗号分隔多个。省略时回落到 @ 或引用对象。
   reason: string
   confirmation_token: string
   duration_seconds: integer|null (minimum=0, maximum=2592000) -- 仅 action=mute。禁言秒数，不是分钟也不是小时：10 分钟=600，1 小时=3600，24 小时=86400，最长 30 天=2592000；0 表示解禁。
   user_ids: array<string> (minItems=1) -- 仅 action=kick。要踢的 QQ 号列表，优于逐个调用。
   blacklist: boolean (default=false) -- 仅 action=kick。true 时踢出并拒绝其后续加群请求（踢黑）。
   special_title: string -- 仅 action=title。要设置的群头衔；空串表示清除。
   duration: integer (default=-1) -- 仅 action=title。头衔有效期秒数；-1 表示永久。
   [分叉] action 枚举按开关裁剪; duration_seconds 仅 allow_ban; user_ids/blacklist 仅 allow_kick; special_title/duration 仅 allow_title

=== qq_group_manage_history_query [inline] 1066B ===
   action: string enum[ban,kick,title,all] (default=all) -- 筛选操作类型；ban 含禁言与解禁，kick 含踢黑。
   view: string enum[events,stats] (default=events) -- events=逐条记录（禁言带 active/expired/unmuted/overridden 状态）；stats=按成员聚合（ban_count、kick_count、total_ban_duration 等）。
   user_id: string
   keyword: string
   min_ban_count: integer (minimum=1) -- 仅 stats 视图：过滤禁言次数下限。
   sort_by: string enum[time,ban_count,kick_count,total_duration,last_action_at] -- events 视图按时间排；stats 视图默认按 ban_count。
   sort_order: string enum[asc,desc] (default=desc)
   limit: integer (minimum=1, maximum=100)
   group_id: string -- 跨群查询的目标群号；仅 Miyu 管理员可用，群聊之外调用时必填。

=== add_active_judgement_skip_qq [inline] 171B ===
  *qq_id: string|integer -- The positive QQ user id to manage.
   [注] remove_active_judgement_skip_qq 复用同一 schema

=== remove_active_judgement_skip_qq [inline] 171B ===
  *qq_id: string|integer -- The positive QQ user id to manage.

=== query_qq_relationship [inline] 154B ===
  *user_id: string -- 要查询关系的 QQ 号

=== load_skill [inline] 175B ===
  *name: string -- The exact skill name from the available skills list.
